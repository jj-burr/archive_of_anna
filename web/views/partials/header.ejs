<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle : 'Archive of Anna' %></title>
  <link rel="stylesheet" href="/css/style.css">
</head>
<body>
  <nav class="navbar">
    <div class="nav-brand">
      <a href="/">Archive of Anna</a>
    </div>
    <div class="nav-links">
      <a href="/search" class="nav-link<%= typeof activePage !== 'undefined' && activePage === 'search' ? ' active' : '' %>">Search</a>
      <a href="/settings" class="nav-link<%= typeof activePage !== 'undefined' && activePage === 'settings' ? ' active' : '' %>">Settings</a>
    </div>
  </nav>
  <main class="container">
